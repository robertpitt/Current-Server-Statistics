import { Meteor } from 'meteor/meteor';

// this is example of making a collection..
import Posts from '../imports/example/app/api/posts.js';  // delete

Meteor.startup(() => {
  // code to run on server at startup
});
