/**
 * @file
 * This envokes the main application..
 */
import React from 'react';
import { Meteor } from 'meteor/meteor';
import { render } from 'react-dom';

// We don't need to envoke a component because we are router is doing that for us.
